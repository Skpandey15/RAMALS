org.gradle.caching=true
# PostgreSQL integration and governance suites intentionally reset one shared database. Project
# parallelism can overlap their test workers despite task ordering, invalidating schema/privilege
# assertions. Keep verification deterministic; test-level parallelism is disabled in build.gradle.
org.gradle.parallel=false
org.gradle.configuration-cache=true
