# MVP-2 architecture decision register

- **Status:** Accepted for MVP-2 implementation
- **Accepted:** 2026-08-22
- **Source package:** `docs/MVP02/RAMALS_MVP2_ADR_Package_v1.0`
- **Governing invariant:** Agents recommend. Deterministic services decide.

This register adopts the decisions in the MVP-2 ADR package as the architecture baseline for
M2-T01. The source DOCX files retain the context, alternatives, rationale, consequences, and revisit
triggers. This repository-native register owns implementation status and task mapping.

| ADR | Accepted decision | Gates |
| --- | --- | --- |
| M2-ADR-001 | Spring Boot/PostgreSQL remain authoritative; agents emit proposals only. | T01-T16 |
| M2-ADR-002 | Persist agent work transactionally with the authoritative change. | T02 |
| M2-ADR-003 | Use at-least-once transport and idempotent, exactly-once business effects. | T03, T09, T12, T14 |
| M2-ADR-004 | Use a PostgreSQL claim/lease dispatcher first; Kafka is deferred. | T02-T03 |
| M2-ADR-005 | Persist immutable resolved provider/model/route/prompt and correlation provenance. | T04 |
| M2-ADR-006 | Spring builds a bounded, authorized, versioned grounded-context package. | T05-T07 |
| M2-ADR-007 | Spring-owned gates validate schema, evidence, semantics, policy, and confidence. | T07, T09, T12 |
| M2-ADR-008 | LangGraph is confined to transient AI-plane execution. | T08, T11, T14 |
| M2-ADR-009 | Workflows are explicit and bounded; agents do not invoke peers freely. | T14 |
| M2-ADR-010 | Deterministic assessment scoring is preferred; AI evaluation is proposal-only. | T11-T13 |
| M2-ADR-011 | Retries are bounded; failures become observable terminal/quarantine state; replay is explicit. | T03, T14-T15 |
| M2-ADR-012 | Domain, durable execution, and transient orchestration state have separate owners. | T02-T16 |
| M2-ADR-013 | Deterministic fakes drive regression; live-provider qualification is selective. | T10, T15-T16 |
| M2-ADR-014 | Logical model routes and their concrete resolution are versioned. | T04 |
| M2-ADR-015 | Correlation identifiers and stable persisted joins are part of every agent contract. | T02-T16 |

## Acceptance conditions and bounded open parameters

The decisions above are accepted. Values that require measurement are deliberately implementation
parameters, not unresolved architecture decisions: lease duration, retry count/backoff, context-size
limits, retention beyond the existing 400-day execution policy, and confidence thresholds. Each
must be configuration with deterministic tests and must be recorded in the implementing task's
evidence. Changing the authority, delivery, state-ownership, or replay semantics requires a new ADR.

## Decisions originating in this repository

Numbered into the same sequence so a reader looking for MVP-2 decisions finds them, but **not part
of the accepted package above** and not covered by its acceptance. Each carries its own status.

| ADR | Decision | Status | Gates |
| --- | --- | --- | --- |
| [M2-ADR-016](M2-ADR-016-provider-execution-contract-capability.md) | Provider capability profile for execution contracts: the synchronous Messages API is Contract B unsupported; Message Batches satisfies every mandatory row except replay-safe admission; execution contract binds to a route and never silently degrades to Contract A or to redispatch; a Contract-B `DIAGNOSE` may be asynchronous, a Contract-A `DIAGNOSE` may not. | **Accepted** 2026-08-27 | T15.2, and any future Contract B task |
| [M2-ADR-017](M2-ADR-017-contract-b-durable-state-and-result-storage.md) | Contract B durable state lives in Spring/PostgreSQL under Flyway and RAMALS-AI stays stateless; only a normalized `diagnostic-proposal.v1` result may be persisted, in dedicated tables rather than `core.ai_execution*`, encrypted at rest, deleted on adoption with a 30-day ceiling, and never containing chain-of-thought or raw provider responses. | **Accepted** 2026-08-27 | Any future Contract B task; gates `V037` |
| [M2-ADR-018](M2-ADR-018-contract-b-result-classification-and-encryption.md) | Contract B result classification, access matrix and encryption at rest: RESTRICTED learner-derived model output, owned by the RAMALS Platform Data Owner role, no reporting/analytics/AI-plane grant, application-layer AES-256-GCM envelope encryption with `pgcrypto` rejected, key access behind a port with no vendor KMS wired, delete-on-adoption with a 30-day ceiling, fail-closed throughout. | **Accepted for MVP/research** 2026-08-27 — not approved for production | Gates `V037` |
| [M2-ADR-019](M2-ADR-019-contract-b-purge-semantics.md) | Contract B purge semantics: the result row is purged entire while execution identity, provider identifiers, `custom_id`, usage and cost survive for audit and reconciliation; two distinct purge paths with a windowed sweep that cannot target a row; a terminal-state test so a live execution is never purged; key destruction as a consequence of purge and never a substitute; and an executable proof before `V037` to resolve the ordering defect in prerequisite 5. | **Proposed** | Unblocks M2-ADR-017 §6 prerequisite 5 |
| [M2-ADR-023](M2-ADR-023-diagnostic-reasoning-is-evidence-not-a-gate.md) | Prerequisite-aware diagnostic reasoning (H1-H7): a weak prerequisite caps/deprioritizes a dependent skill's selection, never excludes it; diagnostic/causal confidence is a distinct, versioned, deterministic construct, never AI-decided and never fed back into mastery computation; H7 (diagnosis verification via reassessment) inherits the already-deferred retention/spaced-reassessment constraint and needs its own decision when scoped. Numbered `023`, not `022`, to avoid colliding with the still-unwritten M1-ADR-010-vs-MVP-2 evaluation-authority ADR. | **Proposed** | Gates H2, H5, H7 (not H1) |
| [M2-ADR-024](M2-ADR-024-hypothesis-driven-probe-relationship-foundation.md) | H4b probe-relationship foundation: `SAME_OBJECTIVE_CONFIRMATION`/`PREREQUISITE_VALIDATION` are read from existing `assessment_item_objective`/`skill_prerequisite`, never re-stored; only `ROOT_CAUSE_PROBE`/`CONTRADICTION_CHECK` get a new `core.diagnostic_probe_relationship` table; H4b code lives in `assessment`, not `diagnosis`, to keep H1's read-only-mastery boundary and its distinct `rootCauses` meaning intact; hypothesis/evidence stay non-authoritative (no `confirmedRootCause`-shaped field, evidence is a three-valued `SUPPORTING`/`CONTRADICTORY`/`INCONCLUSIVE` outcome, never a raw boolean); this ADR authorizes the foundation only, not `DIAGNOSTIC_SELECTION_V5`. §5 (added on review of PR #251): more than one candidate target objective is surfaced as `AMBIGUOUS_TARGET_OBJECTIVE`, never picked by an arbitrary tie-break. | **Proposed** | Gates the H4b foundation PR; does not authorize `DIAGNOSTIC_SELECTION_V5` |
| [M2-ADR-025](M2-ADR-025-hypothesis-driven-probe-runtime-selection.md) | `DIAGNOSTIC_SELECTION_V5` runtime: composition is V3 → V4 → V5 → frozen V2; V5 is a pool restriction + skill-priority adjustment, never a V2 code change, so V3's band cap is never silently undone and the probe quota (`MAX_HYPOTHESIS_PROBES_PER_PACKET = 1`) is enforced structurally; trigger eligibility is exactly "incorrect response in the single immediately-preceding COMPLETED same-version attempt, first miss by presentation_order, first relationship type by a fixed priority order, that resolves CANDIDATES_AVAILABLE" -- not "unexpectedness"; V5 overrides V4 on a shared skill because composition runs V5 last; ambiguity/no-items/all-exposed stay non-actionable, never arbitrated or converted to bank exhaustion; provenance is one new additive table, `core.diagnostic_probe_relationship` untouched; one new `SelectionReason` (`HYPOTHESIS_DRIVEN_PROBE`), not four. §8a (added on review, before merge): provenance-row consistency (source item really in source attempt, objectives really tag the right items, authorizing relationship required/forbidden per type and, when present, must exist/be PUBLISHED/exactly match) is enforced by composite FKs, one CHECK, and a trigger lookup at the database boundary, not trusted from the application writer alone. | **Proposed** | Gates the H4b runtime-integration PR |
| [M2-ADR-026](M2-ADR-026-granular-diagnostic-ontology-foundation.md) | Granular diagnostic ontology foundation: `Concept`/`Sub-concept` (`core.diagnostic_node`) are a content-driven, optional refinement of exactly one `LearningObjective` each -- never `learning_objective` rows themselves, never counted in `objectiveCoverage` or mastery, no third nesting level; `Misconception` is a separate, orthogonal entity with a DB-enforced exclusive-arc target (an objective, concept, or sub-concept) and its own `DRAFT`/`PUBLISHED` lifecycle, immutable once published; a wrong-option mapping ties one incorrect `SINGLE_CHOICE` option to one misconception and may only publish once its misconception is itself published; a new, separate `MisconceptionEvidenceOutcome` classifier governs evidence semantics and never modifies `HypothesisEvidenceOutcome`; V1 uses local, DB-enforceable exclusive-arc typed references, explicitly not frozen as the permanent representation for every future diagnostic subject type; foundation only -- no `core.diagnostic_node_relationship`, no granular provenance/confidence persistence, no wiring into `DiagnosticService`/`DiagnosticSubmissionService`/selection; a future runtime/provenance/confidence milestone needs its own separately reviewed design, with an additional ADR required only if it introduces a decision not already governed here or elsewhere. | **Proposed** | Gates the granular-diagnostic-ontology foundation PR; does not authorize runtime/provenance/confidence integration |
| [M2-ADR-027](M2-ADR-027-granular-diagnostic-runtime-evidence-capture.md) | Granular diagnostic runtime: misconception evidence is captured immutably at event time (anchored to `core.assessment_response.created_at`, the most precise already-immutable timestamp available) and never silently reinterpreted when new mappings publish later; absent adaptive targeting, every event-time-eligible misconception on the item is evaluated, so one response may yield multiple observations (including more than one `SUPPORTING` when an option validly maps to more than one misconception); each observation's provenance anchors to the exact `core.assessment_response` row (not a reconstructed attempt/item pair) and cites the *complete* event-time-eligible mapping set for that misconception, each citation independently proving it predates the response; an observation's own outcome is independently re-derived from authoritative facts alone, never from its own (not-yet-existing at insert time) provenance children, so incomplete provenance from an implementation defect can never produce semantically false evidence; every row carries the mandatory, DB-`CHECK`-constrained `MISCONCEPTION_EVIDENCE_V1` policy identifier; capture is atomic with submission, rolling back the whole transaction on failure; no change to mastery/`objectiveCoverage`/progression, H4b/H5, or `DIAGNOSTIC_SELECTION_V2-V5`; no granular confidence aggregation or adaptive misconception-driven selection authorized here. | **Proposed** | Gates the granular-diagnostic-runtime-evidence-capture PR; does not authorize granular confidence or adaptive selection |
| [M2-ADR-028](M2-ADR-028-granular-diagnostic-confidence.md) | Granular diagnostic confidence: aggregation identity is `(learner_id, misconception_id)` -- a semantic decision (a `PUBLISHED` misconception is one immutable governed belief), not an artifact of V058's schema; cross-assessment-version aggregation for the same `misconception_id` is deliberately authorized, since publication immutability makes it the same object of evaluation regardless of which content produced the evidence, and never merges across distinct ids; snapshot-event identity is `(attempt_id, misconception_id)`, distinct from the aggregation identity; exactly one append-only confidence snapshot is persisted per misconception actually affected by a submission (not one per individual evidence observation), computed after the whole per-response loop from the complete accumulated evidence, inside the same transaction; a historical snapshot is bound permanently to the exact evidence set it was computed from and is never revisited or reinterpreted when later attempts add more evidence -- deliberately no trigger ties a snapshot's persisted counts to a *live, unscoped* re-aggregation, but a `DEFERRABLE INITIALLY DEFERRED` constraint trigger, evaluated at commit and scoped strictly to each row's own cited provenance set, does require a row's persisted counts to exactly equal the aggregated outcomes of the evidence it itself cites (added on review of PR #256, closing a gap the first version left open: a row could otherwise persist counts inconsistent with zero, incomplete, or wrong-category provenance); provenance is an explicit join table citing the complete contributing evidence set including `INCONCLUSIVE`; `DiagnosticConfidenceCalculatorV1`/`DiagnosticConfidenceInputs`/`Result`/`Band` are reused directly and unchanged, no new calculator, no adapter; a persisted band is evidence strength only, never mastery/root-cause/progression authority; strictly separate from H5's own hypothesis-tuple confidence stream, mastery, and progression. | **Proposed** | Gates the granular-diagnostic-confidence PR; does not authorize adaptive selection, mastery integration, or any change to H5's own confidence stream |
| [M2-ADR-029](M2-ADR-029-granular-diagnostic-report.md) | Granular diagnostic report (H6): a deterministic, read-only composition layer, `@Transactional(readOnly = true)` throughout, no persisted report table; two report identities -- Current Domain Diagnostic Report `(learner_id, domain_code)` and Attempt Diagnostic Report `(learner_id, attempt_id)`, the latter never described as "learner state as of this attempt" (it is only what that exact attempt itself produced), its candidate set answered from G2 evidence joined to that exact attempt's own responses -- never from which misconceptions happen to have a G3 snapshot for it, which would conflate "no snapshot yet" with "no evidence" (corrected on review of PR #257); a true historical/as-of-attempt learner-state report is explicitly deferred; V1 composes G2 evidence, G3 confidence, ontology context, and mastery context only -- H5 hypothesis findings are out of scope; mastery context is exposed only by the Current Domain report, never the Attempt report (also corrected on review of PR #257: an Attempt report never bundles today's mastery reading with a possibly old attempt's own findings); H6 never invokes `DiagnosticConfidenceCalculatorV1` (a finding with evidence but no persisted G3 snapshot is `NOT_ASSESSED`, never synthesized); ontology ancestry is display-only context, never structural nesting; four distinct zero-data states (`NO_EVIDENCE`, absent-if-unevidenced, `NOT_ASSESSED`, `ASSESSED` with band `INSUFFICIENT_EVIDENCE`) are never blurred; learner sees findings/evidence/confidence/mastery, admin additionally sees exact confidence/evidence provenance ids, no new RBAC role; all four endpoints are `Cache-Control: no-store`; latest-G3-snapshot selection reuses the already-established `ORDER BY created_at DESC, id DESC` convention (`AssessmentRepository.findMostRecentCompletedAttempt`, `AdminAuditQueryRepository`) rather than introducing a new G3 monotonic version counter. | **Proposed** | Gates the granular-diagnostic-report PR; does not authorize H5 reporting, a true historical/as-of-attempt report, or any new RBAC role |
| [M2-ADR-030](M2-ADR-030-longitudinal-evidence-projection.md) | Longitudinal evidence projection (H7): a deterministic, read-only projection, computed on read, no migration; identity `(learner_id, misconception_id)`, reusing G3's own aggregation identity; baseline is the deterministically selected first eligible G3 snapshot for the pair (`supporting_count + contradictory_count > 0`) under the governed `created_at ASC, id ASC` repository ordering -- explicitly a fixed evidentiary anchor, never a causal/generation-first claim, since `UuidV7.generate()`'s own tiebreak bits are drawn from `SecureRandom` with no monotonic counter (corrected on review of PR #258); its own direction is irrelevant to every later state -- `S=0,C=1` is as valid a baseline as `S=1,C=0`; `LongitudinalDataStatus` (`NO_BASELINE`/`HAS_BASELINE`) is a distinct, independent fact from `LongitudinalEvidenceState` and is never conflated with it -- `NO_BASELINE` is never reported as `NO_LATER_EVIDENCE`, and the single-misconception detail endpoint returns HTTP 200/`NO_BASELINE` for an existing, unevidenced-to-baseline misconception, never a 404 and never a manufactured state; post-baseline evidence (`E_post`) is the exact evidence-observation-id set difference against the baseline's own permanent cited provenance, never a timestamp comparison; a new, separately-named, separately-frozen policy `LONGITUDINAL_EVIDENCE_V1` (a pure sign/existence classifier, five states, no numeric threshold, no probability) deliberately does not reuse and never modifies the frozen `DiagnosticConfidenceCalculatorV1`, since a heavily-supported cumulative G3 band can absorb real post-baseline contradictory evidence without ever moving; `confidenceCoverage` (`CURRENT`/`STALE_RELATIVE_TO_LATER_EVIDENCE`) is meaningful, and computed by exact provenance, only when a persisted G3 snapshot exists at all -- `null`, never `CURRENT`, when none does; `ORDER BY created_at ASC, id ASC` for the detail endpoint's post-baseline evidence list is documented as a deterministic presentation ordering only, proven (by inspecting `UuidV7.generate()`'s own `SecureRandom`-drawn tiebreak bits) not to be a causal/generation-order guarantee within a timestamp tie, so no H7 outcome ever depends on evidence sequence; forbidden terminology (`VERIFIED`/`CONFIRMED`/`RESOLVED`/`CURED`/`RECURRENCE`/`REGRESSION`/`REVERSAL`/`ROOT_CAUSE`) is never produced; no mastery, no H5 reads, and H6's `DiagnosticReportService`/`Controller`/`AdminController`/`Response`/`Repository` are all left untouched -- H7 implements its own small, bounded, deliberately duplicated ontology/context reads rather than sharing surface with H6 during this governed milestone. | **Proposed** | Gates the longitudinal-evidence-projection PR; does not authorize order-sensitive interpretation, mastery integration, H5 integration, persisted H7 events, or any H6 change |
| [M2-ADR-031](M2-ADR-031-delegated-learner-context-for-ai-initiated-access.md) | Delegated learner context for AI-initiated platform access: governs the reverse call direction MCP introduces (`ramals-ai`/LangGraph calling into Java), distinct from and unblocked by M1-ADR-003's own Java-to-`ramals-ai` workload identity, which is untouched; a valid `ramals-ai` workload token proves only "this caller is the authenticated workload," never "this workload may access learner X" -- so a learner-scoped MCP request MUST present both workload authentication (unchanged, `aud=ramals-ai`) and a new, short-lived, signed delegated-context credential (`aud=ramals-mcp`, a new dedicated audience reusing the same audience-separation mechanism `application.yml`'s existing `spring.security.oauth2.resourceserver.jwt.audiences` already enforces) that Java itself issues and verifies (self-contained, single-issuer, single-verifier -- deliberately not a second identity issuer in M1-ADR-003's own rejected sense, since it asserts no service identity at all, only a scoped capability grant riding alongside the unchanged workload leg); the token carries interactionId, opaque learner scope, domain scope, and an explicit allowlisted capability-name array (never a wildcard), and MUST NOT carry mastery/G2/G3/H5/H7/progression state or any secret; an MCP request parameter can never override or supply the learner identity, which is derived solely from the verified token; lifetime is bounded to the authorizing interaction, never reusable as a login or refresh credential; admin-scoped AI-initiated access is explicitly deferred, not silently assumed; Keycloak-issued delegation via standard token exchange is recorded as the preferred long-term alternative, not chosen now because the realm does not currently enable that feature. | **Proposed** | Gates any MCP (or future non-MCP) AI-initiated call into Java's learner-scoped capabilities; does not authorize admin-scoped AI-initiated access, which needs its own separate decision |
| [M2-ADR-032](M2-ADR-032-governed-advisory-ai-diagnostic-probe-proposal-boundary.md) | Governed advisory AI diagnostic-probe proposal boundary: distinguishes DIAGNOSIS/CLASSIFICATION (asserting what is true of the learner -- forever exclusively Java's, unchanged) from EVIDENCE-ACQUISITION RECOMMENDATION (an advisory, non-learner-facing claim about which additional governed evidence would discriminate remaining diagnostic uncertainty); accepted with a strict authority boundary -- an agent that has read governed H6/H7 MCP evidence may propose at most one bounded next diagnostic-probe candidate per interaction, referencing only governed evidence and authored misconceptions actually supplied to that interaction (`E_proposed ⊆ E_allowed`; `M_allowed` independently re-validated by Java), and the proposal has zero operational effect unless Java's own independent, deterministic, fail-closed gate (a minimum 15-point future validation list, including schema/version, learner/interaction/domain binding, target/evidence/provenance existence and authorization, probe eligibility, and independent accept/reject) accepts it; explicitly does not overturn M2-ADR-023's rejection of AI-ranked/classified root-cause hypotheses (the recommendation never ranks, classifies, or is learner-facing) and explicitly does not reopen M2-ADR-025's rejection of same-attempt, LLM-driven probe selection (`DIAGNOSTIC_SELECTION_V1`-`V5` are untouched; an accepted recommendation may only inform a subsequent diagnostic interaction, never mutate an assessment attempt in progress); G3/H7/ontology semantics are preserved verbatim and an advisory proposal may never restate them as probability, resolution, or root-cause truth; MCP remains read-only, no MCP-4 authorized; no AI-generated assessment item authorized; the existing `DiagnosticAssessmentProposal` (v1.0) contract is confirmed unable to represent this role and is explicitly left unmodified -- a separate, versioned proposal-contract PR is required before any implementation. | **Proposed** | Fixes the constraint any future diagnostic-probe-proposal contract, Java validation gate, or Python reasoning PR must be reviewed against; does not itself authorize any runtime change, any proposal contract change, or MCP-4 |
| [M2-ADR-033](M2-ADR-033-misconception-relationship-graph.md) | Misconception relationship graph: adds authored, non-authoritative `MISCONCEPTION_RELATED` (misconception↔misconception) and `MISCONCEPTION_PREREQUISITE_LINK` (misconception→prerequisite) edges, plus a bounded, read-only, authored-knowledge-only queryable graph surface, extending M2-ADR-026 §8's deferred `diagnostic_node_relationship`; a misconception stays orthogonal -- never a `skill`, never a `diagnostic_node`, no third `Concept`/`Sub-concept` level; edges carry no learner state and no confidence, so `MISCONCEPTION_EVIDENCE_V1`, G3 (`DIAGNOSTIC_CONFIDENCE_V1`), H6, H7 and every frozen calculator are unchanged and no parallel confidence model is introduced; DB-enforced `DRAFT`/`PUBLISHED` immutability, published-endpoint-only, `SPECIALISES` acyclicity; foundation/representation only -- no wiring into `DiagnosticService`/`DIAGNOSTIC_SELECTION_V1`-`V5`/`ProbeRelationshipResolver`, and no adaptive misconception-driven selection; `V057`-`V059` immutable, edges are additive later migrations. | **Proposed** | Gates the misconception-relationship-graph foundation PR; does not authorize adaptive selection, a learner-scoped graph overlay, or any runtime wiring |
| [M2-ADR-034](M2-ADR-034-information-gain-probe-selection.md) | Information-gain diagnostic probe selection (design only): a future `DIAGNOSTIC_SELECTION_V6` supersedes **only `V5`'s final candidate-tiebreak step** -- replacing "first eligible by fixed priority" with an information-gain ranking over the *same* eligible-candidate set `V5`'s relationship resolution already produces -- and changes nothing else: the `V3→V4→V5→frozen V2` order, `V1`-`V4` code, `MAX_HYPOTHESIS_PROBES_PER_PACKET` (still exactly one probe, still enforced by restricting `V2`'s pool), and `core.diagnostic_probe_relationship`/`core.diagnostic_probe_provenance` are untouched; this is the exact supersession M2-ADR-025's own revisit trigger authorized. A named, versioned, frozen `INFORMATION_GAIN_V1` construct computes an explicit deterministic hypothesis posterior/uncertainty and a per-probe deterministic score -- an expected-information-gain value **only** when its outcome model (built from `HypothesisEvidenceOutcome`'s three-valued mapping + the posterior, with no learned probabilities) is itself frozen, otherwise a non-expectation deterministic discrimination score -- with a deterministic total order, from already-authoritative inputs only, following the `EngineVersionFreezeTests` discipline (M2-ADR-023 §2); the posterior is a separate stream that never feeds `WEIGHTED_MASTERY_V1`/`EVIDENCE_CONFIDENCE_V2`/`MASTERY_STATUS_POLICY_V2`/G3/H5/H7; an LLM never computes the posterior, the score, the ranking, or the selected probe -- the sole permitted AI role is M2-ADR-032's advisory ≤1-candidate proposal, admitted or rejected by Java's deterministic fail-closed gate for its own advisory/audit purposes only (gate acceptance does not by itself admit a proposal into any scored or ranked pool -- see Amendment 2); no `V6` implementation, migration, contract, or `SelectionReason` value in this PR. **Amendment 1 (2026-09-10)** ratifies and authorizes the inert Step-1 construct `HYPOTHESIS_UNCERTAINTY_V1` — a deterministic normalized relative hypothesis-uncertainty distribution over the already-authorized candidate set, per-interaction governed probe evidence only, `DiagnosticConfidenceCalculatorV1` reused verbatim for the band, ordinal-linear band weights `LOW=1/MODERATE=2/HIGH=3`, `INSUFFICIENT_EVIDENCE` excluded from the distribution behind an explicit `APPLICABLE`/`INSUFFICIENT_EVIDENCE`/`NOT_APPLICABLE` status, `BigDecimal` scale 4 `HALF_EVEN` with largest-remainder residual allocation and a total canonical order on hypothesis identity, fail-closed input validation (duplicate hypothesis / unknown-hypothesis evidence / cross-domain / cross-interaction / duplicate observation id) and observation-id evidence de-duplication, frozen with eleven golden vectors; no M2-ADR-033 graph or H7 input; still no `DIAGNOSTIC_SELECTION_V6`/`INFORMATION_GAIN_V1` code, migration, contract, or `SelectionReason`. **Amendment 2 (2026-09-11)** resolves the fork Amendment 1 left open for `INFORMATION_GAIN_V1`: RAMALS has deterministic outcome *classification* (`HypothesisEvidenceOutcome`) but no outcome *probability*, and none may be invented, so true expected information gain is not defensible; Amendment 2 instead ratifies and authorizes the inert Step-2 construct **`HYPOTHESIS_DISCRIMINATION_V1`** (the accurate name — not information gain) — for each candidate probe (drawn **only** from V5's own already-governed candidate-item resolution, one hypothesis at a time; an M2-ADR-032-accepted proposal is **not** eligible here and stays within its own advisory/audit boundary — promoting it into scoring/selection would require a separate, explicit ADR), the total variation distance between the two `HYPOTHESIS_UNCERTAINTY_V1` distributions hypothetically resulting from its two deterministically reachable outcomes (`SUPPORTING`/`CONTRADICTORY`, or a trivial single `INCONCLUSIVE` world scoring `0.0000`); consumes `HYPOTHESIS_UNCERTAINTY_V1` and its own input context verbatim, recomputing no band/weight/normalization; `BigDecimal` scale 4 `HALF_EVEN`; deterministic score-then-canonical-order-then-probe-id ranking; fail-closed validation; no M2-ADR-033 graph weighting, no new candidate-probe discovery; a documented V1 limitation that it does not model probe-specific psychometric quality (two scoreable probes on the same hypothesis can tie); eleven golden vectors frozen; still no `DIAGNOSTIC_SELECTION_V6`, migration, contract, or `SelectionReason`. | **Proposed** (Amendment 1 authorizes inert Step 1; Amendment 2 authorizes inert Step 2) | Fixes the constraints any future `DIAGNOSTIC_SELECTION_V6` design and implementation must be reviewed against; invokes M2-ADR-025's revisit trigger; Amendments 1–2 authorize the inert `HYPOTHESIS_UNCERTAINTY_V1` (Step 1) and `HYPOTHESIS_DISCRIMINATION_V1` (Step 2) foundations and nothing more |
| [M2-ADR-035](M2-ADR-035-intervention-outcome-and-effectiveness.md) | **Deferred roadmap stub** -- intervention outcome & effectiveness: reserves the governed chain diagnosis → intervention → pre-intervention evidence → reassessment → post-intervention evidence → outcome (`POSITIVE`/`NEGATIVE`/`INSUFFICIENT_EVIDENCE`) → deterministic `INTERVENTION_EFFECTIVENESS_V1`; append-only, provenance/correlation-linked, writes the common `ledger.evidence`, effectiveness is a separate stream that never feeds mastery or G3 (M2-ADR-023 §2); **no reinforcement learning, no ML effectiveness model, no automatic intervention selection**; initial objective is governed evidence collection only; depends on the still-unscoped M2-ADR-023 §3 retention/spaced-reassessment decision for its reassessment leg. Authorizes no schema, no code, no implementation. | **Deferred** | Reserves the boundary and number; a separately reviewed full ADR and design PR precede any work; horizon MVP-3 foundation |
| [M2-ADR-036](M2-ADR-036-production-simulation-evidence-model.md) | **Deferred roadmap stub** -- production-simulation evidence model: reserves incident-simulation / debugging / architecture-scenario / secure-lab / operational-troubleshooting evidence as a first-class modality that writes the SAME `ledger.evidence` and flows through the SAME deterministic mastery/confidence/diagnostic architecture -- never an independent scoring system; captures structured reasoning-trace evidence (observe → hypothesize → investigate → cite evidence → decide → remediate), not free prose; deterministic or human/hybrid scoring -- AI must not become an authoritative scorer unless a future ADR explicitly changes that boundary (M1-ADR-010/M2-ADR-010); **hard dependency: M2-ADR-022 must resolve free-text/rubric evaluation authority first** (it also gates `SHORT_ANSWER`/`USE_CASE`); secure-lab execution is a new security surface that must not weaken the Zero Trust or learner-privacy boundaries. Authorizes no schema, no code, no implementation. | **Deferred** | Reserves the boundary and number; blocked on M2-ADR-022; a separately reviewed full ADR and design PR precede any work; horizon MVP-3 / secure-lab evolution |
| [M2-ADR-037](M2-ADR-037-organizational-capability-aggregation-and-privacy.md) | **Deferred roadmap stub** -- organizational capability aggregation & privacy boundary: reserves aggregation of governed individual competency evidence into organizational capability intelligence where **organizational capability ≠ `AVG(individual mastery)`** -- a deterministic, versioned, reproducible engine over `ledger.mastery_snapshot`/`ledger.evidence`/confidence/recency accounting for at least competency coverage, evidence sufficiency, confidence, recency, proficiency distribution, critical skills, minimum-evidence thresholds, minimum-population/privacy thresholds, organizational hierarchy, RBAC and tenant isolation; individual learner privacy never weakens (M1-ADR-013, product-vision guardrail); **a multi-tenancy architecture decision is a mandatory predecessor**. Authorizes no schema, no code, no implementation. | **Deferred** | Reserves the boundary and number; blocked on a multi-tenancy ADR and a privacy/legal review; a separately reviewed full ADR and design PR precede any work; horizon MVP-4/5+ |

M2-ADR-016 is the capability gate the Contract B design document requires before implementation. It
was Proposed pending one product decision — whether a Contract-B `DIAGNOSE` may become asynchronous
— which was answered on review of PR #161 and is recorded in its §6 with the six rules bounding the
grant. Asynchrony is confined to Contract-B routes and is not a relaxation of M1-ADR-001.

**Addendum A (2026-08-27)** records the OpenAI Responses background-mode evaluation its second
revisit trigger called for, since `gpt-4.1-2025-04-14` is already an approved alternate binding.
Neither provider documents replay-safe admission. OpenAI loses on the lost-acknowledgement path:
its requests can carry a caller-supplied `metadata` label, but responses cannot be enumerated or
searched, so an unacknowledged execution is permanently unreachable and a duplicate is
undetectable — which makes the T15 cost-evidence scenario unbuildable. Anthropic Message Batches remains the selected path, and still provides no
provider-level exactly-once. The addendum adds evidence and reverses no decision.

**Acceptance authorizes design and construction, not traffic and not schema.** No Contract-B
production route is activated. The two further decisions M2-ADR-016 recorded as consequences —
where Contract B durable state lives, given M2-ADR-008 and M2-ADR-012, and whether model output may
be stored at all, given the `V035` invariant — are **both closed by M2-ADR-017**.

M2-ADR-017 keeps the AI plane stateless and puts Contract B durable state in Spring/PostgreSQL
under Flyway; permits only a normalized `diagnostic-proposal.v1` result, in dedicated tables that
leave every `core.ai_execution*` and `V035` structural-redaction guarantee literally intact;
prohibits chain-of-thought and raw provider responses outright; and requires encryption at rest,
deletion in the adoption transaction, and a 30-day ceiling chosen against the provider's own
retention window. It authorizes no schema and no code: its §6 lists seven prerequisites, of which
1–5 gate `V037` and 6–7 gate any route activation.

**Contract B status as of 2026-08-27: `V037` is no longer blocked on governance for the
MVP/research environment.** Prerequisite 2 is satisfied by the [amended Definition of
Done](../release/mvp2-contract-b-definition-of-done.md). Prerequisites 3 and 4 are satisfied for
that scope by M2-ADR-018, which carries the classification, access matrix, isolation, audit,
encryption architecture, key lifecycle and failure semantics, and now records the sign-off — Sunil
Pandey as Platform Data Owner and interim Key Custodian, classification `RESTRICTED —
LEARNER-DERIVED MODEL OUTPUT`.

**That approval is scoped to MVP/research and does not travel to production.** A production
deployment requires both roles to be reassigned under the deploying organisation's governance, and
the Data Owner and Key Custodian should then be different people — they are the same individual
here, which M2-ADR-018 records as accepted for a single-maintainer research environment rather than
as sound segregation of duties.

**Prerequisite 1 was qualified on 2026-08-27** against the real Anthropic API — one Message Batch
via the #170 adapter, with separate-process recovery by durable `msgbatch_…` identity, `custom_id`
correlation and exactly one submission. See the [prerequisite 1 qualification
evidence](../release/mvp2-contract-b-prerequisite-1-qualification.md). It qualifies **provider
capability only**: Anthropic still documents no replay-safe admission, cancellation was not
attempted and must not be represented as proven, and the lost-acknowledgement window was not
exercised.

**Prerequisite 5 was satisfied on 2026-08-27** by an [executable purge
proof](../release/mvp2-contract-b-purge-proof.md) — eight behaviours proven and five negative
controls caught against an isolated throwaway schema, per M2-ADR-019 §6.

~~`V037` remains blocked on M2-ADR-018 criteria 3, 4, 5 and 9 (the key-provider port, the envelope
format, fail-closed behaviour and log hygiene), none of which is done.~~ Criteria 6, 7 and 8 were
reclassified by M2-ADR-019 as `V037` completion criteria.

**Superseded 2026-08-28.** All nine M2-ADR-018 criteria are satisfied and `V037` has shipped —
the key-provider port in `#177`, the envelope and fail-closed behaviour in `#178`, and the grants,
transactional adoption and purge in `#179`. The durable lifecycle followed in `#181` and its
crash-recovery qualification in `#182`.

`V037` shipping is not Contract B being available, and neither is the lifecycle. No route is bound
to Contract B, the reconciliation worker is off, and four Definition-of-Done criteria remain unmet —
see the [MVP-2 closure assessment](../release/mvp2-closure-assessment.md). Approving the model was
not the same as building it; building it is not the same as qualifying it.

**Contract A remains the default and current execution contract.** Every route is on Contract A, it
stays correct and supported for routes that never move, and this decision neither deprecates it nor
schedules its removal.

## Supersession and compatibility

- M2-ADR-002 supersedes `AFTER_COMMIT` as the correctness boundary for agent delivery. An
  `AFTER_COMMIT` listener may remain only as a best-effort wake-up optimization after T02.
- M2-ADR-005 extends M1-ADR-005 without rewriting historical `core.ai_execution` rows.
- M2-ADR-010 preserves M1-ADR-010: AI evaluation remains formative/proposal-only unless a later ADR
  deliberately changes that product boundary.
- Existing contract v1.0 remains compatible. MVP-2 contracts use independent version `1.0` schemas
  and are introduced additively.
- M2-ADR-033 and M2-ADR-034 extend M2-ADR-024/025/026 (and reaffirm M2-ADR-032) without modifying
  them; migrations `V050`–`V059` and every existing frozen calculator are unchanged. M2-ADR-034,
  **as amended 2026-09-10 (Amendment 1)**, ratified and authorized one inert new frozen construct —
  `HYPOTHESIS_UNCERTAINTY_V1`, a deterministic normalized relative hypothesis-uncertainty
  distribution reusing `DiagnosticConfidenceCalculatorV1` verbatim — as **Step 1** (implemented
  2026-09-11). **As further amended 2026-09-11 (Amendment 2)**, it ratifies and authorizes a second
  inert frozen construct — `HYPOTHESIS_DISCRIMINATION_V1` (not `INFORMATION_GAIN_V1`: no outcome
  probability exists or may be invented, so the construct is a non-expectation deterministic
  discrimination score, not an expectation), a total-variation-distance score over
  `HYPOTHESIS_UNCERTAINTY_V1`'s own two hypothetically-reachable post-outcome distributions per
  candidate probe — as **Step 2** (design ratified; implementation pending). Together the two
  amendments still authorize **no** `DIAGNOSTIC_SELECTION_V6` code, no migration, and no contract
  change; `DIAGNOSTIC_SELECTION_V1`–`V5` and their composition order are untouched and no selector
  consumes either construct.
- M2-ADR-035, M2-ADR-036 and M2-ADR-037 are **Deferred roadmap stubs**: they capture problem,
  intent, invariants, dependencies, non-goals, and activation criteria only, and authorize no
  schema, contract, code, or implementation. Each is superseded by its own full ADR when the owning
  milestone is scheduled.
- None of M2-ADR-033 through M2-ADR-037 supersedes or amends M2-ADR-023 through M2-ADR-032; all are
  additive. The stage-by-stage status of the long-term intelligence loop is mapped in
  [`docs/architecture/target-intelligence-loop.md`](../architecture/target-intelligence-loop.md).

## Implementation evidence

- M2-ADR-002 persistence boundary: [M2-T02 transactional outbox](../release/mvp2-t02-transactional-outbox.md)
- M2-ADR-003/004/011 delivery boundary: [M2-T03 durable dispatcher](../release/mvp2-t03-durable-dispatcher.md)
- M2-ADR-005/014 provenance boundary: [M2-T04 AI provenance v2](../release/mvp2-t04-ai-provenance-v2.md)
- M2-ADR-006 context boundary: [M2-T05 GroundedContext v1](../release/mvp2-t05-grounded-context-contract.md)
