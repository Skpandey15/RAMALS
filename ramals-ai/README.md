# RAMALS AI service placeholder

This directory reserves the MVP-1 Python/FastAPI boundary. MVP-0 has no Python runtime, dependency, container, or direct database access. Future agents will consume controlled Spring APIs and return validated proposals; they will never mutate authoritative mastery or progression state.

